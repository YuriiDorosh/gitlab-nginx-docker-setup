# ─────────── Базове ───────────
GITLAB_HOST=gitlab.local
TZ=Europe/Kyiv

# root-адміністратор (буде створений при першому старті)
GITLAB_ROOT_PASSWORD=ChangeMe_123!
GITLAB_ROOT_EMAIL=root@${GITLAB_HOST}

# ─────────── Gmail SMTP ───────────
# Увімкнути e-mail-нотифікації — true / false
SMTP_ENABLED=true
SMTP_USER=gitlab.noreply@gmail.com          # повна адреса
SMTP_PASS=xxxxxxxxxxxxxxxx                  # App Password або OAuth token
SMTP_ADDRESS=smtp.gmail.com
SMTP_PORT=587
SMTP_DOMAIN=gmail.com

# ─────────── IP-allowlist (опційно) ───────────
ALLOWED_IPS=203.0.113.0/24,198.51.100.10/32


BACKUP_DIR=./backups